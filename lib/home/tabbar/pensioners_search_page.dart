import 'package:flutter/material.dart';

class PensionersSearchPage extends StatelessWidget {
  const PensionersSearchPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('seniors'),
      ),
    );
  }
}
