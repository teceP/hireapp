import 'package:flutter/material.dart';

class BeautySearchPage extends StatelessWidget {
  const BeautySearchPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(child: Center(child: Text('beauty'),),);
  }
}