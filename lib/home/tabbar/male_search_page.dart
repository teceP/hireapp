import 'package:flutter/material.dart';

class MaleSearchPage extends StatelessWidget {
  const MaleSearchPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('male'),
      ),
    );
  }
}
