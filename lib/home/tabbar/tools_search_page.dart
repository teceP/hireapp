
import 'package:flutter/material.dart';

class ToolsSearchPage extends StatelessWidget {
  const ToolsSearchPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(child: Center(child: Text('tools'),),);
  }
}