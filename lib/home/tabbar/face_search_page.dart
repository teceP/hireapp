
import 'package:flutter/material.dart';

class FaceSearchPage extends StatelessWidget {
  const FaceSearchPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(child: Center(child: Text('face'),),);
  }
}